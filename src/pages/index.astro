---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navigation from '../components/ui/Navigation.astro';
import Footer from '../components/ui/Footer.astro';
import { t, getAlternateLocales } from '../i18n/translations';

const locale = 'en';

const categories = [
  { icon: '📊', name: 'Finance', slug: 'finance' },
  { icon: '🏋️', name: 'Health', slug: 'health' },
  { icon: '💻', name: 'Tech', slug: 'tech' },
  { icon: '📐', name: 'Science', slug: 'math' },
  { icon: '🌍', name: 'Travel', slug: 'travel' },
  { icon: '🎮', name: 'Fun', slug: 'fun' },
];
---

<BaseLayout
  title="FluxFact — Free Interactive Tools & Calculators"
  description="Discover free interactive calculators, data visualizations, and tools on trending topics. Finance, health, tech, science — explore, calculate, and share."
  ogImage="/og/default.png"
  keywords={['free calculator', 'online tools', 'data visualization', 'interactive tools', 'trending topics']}
  alternateLocales={getAlternateLocales('/')}
  locale="en"
>
  <Navigation locale={locale} currentPath="/" />

  <main class="pt-16">
    <!-- ===== HERO ===== -->
    <section class="relative overflow-hidden min-h-[85vh] flex items-center">
      <!-- Ambient orbs -->
      <div class="orb-blue w-[600px] h-[600px] -top-40 -left-40 animate-pulse-glow" />
      <div class="orb-purple w-[500px] h-[500px] top-20 right-[-10%] animate-pulse-glow" style="animation-delay: 1s" />
      <div class="orb-cyan w-[400px] h-[400px] bottom-0 left-1/3 animate-pulse-glow" style="animation-delay: 2s" />

      <!-- Noise texture -->
      <div class="noise absolute inset-0" />

      <!-- Grid lines overlay -->
      <div class="absolute inset-0 opacity-[0.02]"
           style="background-image: linear-gradient(rgba(255,255,255,0.1) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.1) 1px, transparent 1px); background-size: 60px 60px;" />

      <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-24 text-center">
        <!-- Badge -->
        <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-white/[0.06] border border-white/[0.08] mb-8 opacity-0 animate-fade-up">
          <span class="w-2 h-2 rounded-full bg-emerald-400 animate-pulse" />
          <span class="text-sm text-txt-secondary">Free. No sign-up required.</span>
        </div>

        <!-- Main heading -->
        <h1 class="heading-display mb-6 opacity-0 animate-fade-up" style="animation-delay: 100ms">
          Discover. <span class="gradient-text">Calculate.</span> Share.
        </h1>

        <!-- Subtitle -->
        <p class="text-lg md:text-xl text-txt-secondary max-w-2xl mx-auto mb-12 leading-relaxed opacity-0 animate-fade-up" style="animation-delay: 200ms">
          {t(locale, 'home.hero.subtitle')}
        </p>

        <!-- Search -->
        <div class="max-w-xl mx-auto opacity-0 animate-fade-up" style="animation-delay: 300ms">
          <div class="relative group">
            <div class="absolute -inset-0.5 bg-gradient-to-r from-accent-blue/20 to-accent-cyan/20 rounded-2xl blur opacity-0 group-hover:opacity-100 transition-opacity duration-500" />
            <div class="relative">
              <svg class="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-txt-muted" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
              </svg>
              <input
                type="search"
                placeholder="Search tools and calculators..."
                class="input-field pl-12 text-lg !rounded-2xl"
                id="hero-search"
              />
            </div>
          </div>
        </div>

        <!-- Quick category pills -->
        <div class="flex flex-wrap justify-center gap-2 mt-8 opacity-0 animate-fade-up" style="animation-delay: 400ms">
          {categories.map((cat) => (
            <a href={`/tools/${cat.slug}/`}
               class="px-4 py-2 rounded-full text-sm text-txt-secondary bg-white/[0.04] border border-white/[0.06] hover:bg-white/[0.08] hover:border-white/[0.12] transition-all duration-200">
              <span class="mr-1.5">{cat.icon}</span>{cat.name}
            </a>
          ))}
        </div>
      </div>
    </section>

    <!-- ===== TRENDING TOOLS ===== -->
    <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
      <div class="flex items-center gap-3 mb-10">
        <span class="w-2.5 h-2.5 rounded-full bg-red-500 animate-pulse" />
        <h2 class="heading-section">{t(locale, 'home.trending')}</h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <!-- BMI Calculator — real tool -->
        <a href="/tools/bmi-calculator/" class="tool-card">
          <div class="flex items-start justify-between mb-5">
            <div class="w-12 h-12 rounded-xl bg-gradient-to-br from-emerald-500/20 to-cyan-500/20 border border-emerald-500/10 flex items-center justify-center text-xl">
              🏋️
            </div>
            <span class="badge-green">Free</span>
          </div>
          <h3 class="text-lg font-semibold text-txt-primary mb-2 group-hover:text-accent-blue transition-colors">
            BMI Calculator
          </h3>
          <p class="text-sm text-txt-secondary leading-relaxed">
            Calculate your Body Mass Index instantly. Visual scale with healthy range indicator.
          </p>
          <div class="mt-4 flex items-center gap-2 text-xs text-txt-muted">
            <span class="px-2 py-0.5 rounded bg-white/[0.04]">Health</span>
            <span class="px-2 py-0.5 rounded bg-white/[0.04]">Calculator</span>
          </div>
        </a>

        <!-- Placeholder cards -->
        <div class="tool-card">
          <div class="flex items-start justify-between mb-5">
            <div class="w-12 h-12 rounded-xl bg-gradient-to-br from-accent-blue/20 to-accent-purple/20 border border-accent-blue/10 flex items-center justify-center text-xl">
              📊
            </div>
            <span class="badge-blue">Coming Soon</span>
          </div>
          <h3 class="text-lg font-semibold text-txt-primary mb-2">Salary Calculator</h3>
          <p class="text-sm text-txt-secondary leading-relaxed">
            Compare your salary across countries and industries. Cost of living adjusted.
          </p>
          <div class="mt-4 flex items-center gap-2 text-xs text-txt-muted">
            <span class="px-2 py-0.5 rounded bg-white/[0.04]">Finance</span>
            <span class="px-2 py-0.5 rounded bg-white/[0.04]">Comparison</span>
          </div>
        </div>

        <div class="tool-card">
          <div class="flex items-start justify-between mb-5">
            <div class="w-12 h-12 rounded-xl bg-gradient-to-br from-accent-pink/20 to-accent-coral/20 border border-accent-pink/10 flex items-center justify-center text-xl">
              🎯
            </div>
            <span class="badge-blue">Coming Soon</span>
          </div>
          <h3 class="text-lg font-semibold text-txt-primary mb-2">Life Expectancy Quiz</h3>
          <p class="text-sm text-txt-secondary leading-relaxed">
            Answer 15 science-backed questions to estimate your life expectancy.
          </p>
          <div class="mt-4 flex items-center gap-2 text-xs text-txt-muted">
            <span class="px-2 py-0.5 rounded bg-white/[0.04]">Health</span>
            <span class="px-2 py-0.5 rounded bg-white/[0.04]">Quiz</span>
          </div>
        </div>
      </div>
    </section>

    <!-- ===== CATEGORIES ===== -->
    <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
      <h2 class="heading-section mb-10">{t(locale, 'home.categories')}</h2>

      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
        {categories.map((cat) => (
          <a href={`/tools/${cat.slug}/`}
             class="glass-card-hover p-5 text-center">
            <span class="text-3xl mb-3 block group-hover:scale-110 transition-transform">{cat.icon}</span>
            <span class="text-sm font-medium text-txt-primary">{cat.name}</span>
          </a>
        ))}
      </div>
    </section>

    <!-- ===== VALUE PROPOSITION ===== -->
    <section class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
      <div class="orb-blue w-[400px] h-[400px] top-0 left-1/2 -translate-x-1/2 opacity-50" />

      <div class="relative glass-card p-8 md:p-14">
        <div class="grid md:grid-cols-3 gap-10">
          <div class="text-center">
            <div class="w-14 h-14 mx-auto mb-5 rounded-2xl bg-accent-blue/10 border border-accent-blue/20 flex items-center justify-center">
              <svg class="w-7 h-7 text-accent-blue" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-txt-primary mb-2">Instant Results</h3>
            <p class="text-sm text-txt-secondary leading-relaxed">All tools run in your browser. No sign-up, no waiting. Just instant answers.</p>
          </div>

          <div class="text-center">
            <div class="w-14 h-14 mx-auto mb-5 rounded-2xl bg-accent-purple/10 border border-accent-purple/20 flex items-center justify-center">
              <svg class="w-7 h-7 text-accent-purple" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-txt-primary mb-2">7 Languages</h3>
            <p class="text-sm text-txt-secondary leading-relaxed">Available in English, German, French, Spanish, Portuguese, Russian, and Japanese.</p>
          </div>

          <div class="text-center">
            <div class="w-14 h-14 mx-auto mb-5 rounded-2xl bg-accent-pink/10 border border-accent-pink/20 flex items-center justify-center">
              <svg class="w-7 h-7 text-accent-pink" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z" />
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-txt-primary mb-2">Share Anywhere</h3>
            <p class="text-sm text-txt-secondary leading-relaxed">Beautiful share cards for every result. One click to share on any social network.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- ===== CTA ===== -->
    <section class="relative overflow-hidden py-24">
      <div class="orb-purple w-[500px] h-[500px] top-0 right-0" />
      <div class="orb-blue w-[400px] h-[400px] bottom-0 left-0" />
      <div class="noise absolute inset-0" />

      <div class="relative z-10 text-center max-w-2xl mx-auto px-4">
        <h2 class="heading-section mb-4">
          Explore all <span class="gradient-text">tools</span>
        </h2>
        <p class="text-txt-secondary mb-8 leading-relaxed">
          Free interactive calculators, visualizations, and tools on the topics that matter to you.
        </p>
        <a href="/tools/" class="btn-primary text-base px-8 py-4">
          Browse All Tools
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
          </svg>
        </a>
      </div>
    </section>
  </main>

  <Footer locale={locale} />
</BaseLayout>

<script>
  // Intersection Observer for scroll reveal
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.style.opacity = '1';
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.1, rootMargin: '50px' }
  );
  document.querySelectorAll('.tool-card, .glass-card-hover').forEach((el) => observer.observe(el));
</script>
